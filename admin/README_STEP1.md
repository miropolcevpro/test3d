# Admin (Step 1)

Read-only skeleton:
- Loads shapes from `../shapes.json`
- Loads palette from Object Storage: `${bucketBase}palettes/<shapeId>.json`
- Displays `items[]` as cards (preview + name + id)

No auth, no uploads, no edits in Step 1.
